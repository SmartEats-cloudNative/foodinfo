server:
  port: 9093


spring:
  profiles:
    active: dev
  application:
    name: FoodCatalogue-service
  datasource:

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect   # <-- valid for Hibernate 6
    # or: org.hibernate.dialect.MySQL8Dialect




